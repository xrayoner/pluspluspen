<Application x:Class="PlusPlusPen.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <Color x:Key="PanelBlueColor">#CCDBF4FF</Color>
        <Color x:Key="PanelBorderColor">#FF3AA6E8</Color>
        <Color x:Key="PanelAccentColor">#FF57C1FF</Color>
        <Color x:Key="PanelActiveGreenColor">#FF3CBD5B</Color>
        <Color x:Key="PanelTextColor">#FF0C2A4F</Color>

        <SolidColorBrush x:Key="PanelBlueBrush" Color="{StaticResource PanelBlueColor}" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="{StaticResource PanelBorderColor}" />
        <SolidColorBrush x:Key="PanelAccentBrush" Color="{StaticResource PanelAccentColor}" />
        <SolidColorBrush x:Key="PanelActiveGreenBrush" Color="{StaticResource PanelActiveGreenColor}" />
        <SolidColorBrush x:Key="PanelTextBrush" Color="{StaticResource PanelTextColor}" />

        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Foreground" Value="{StaticResource PanelTextBrush}" />
            <Setter Property="Background" Value="#F0FFFFFF" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1.2" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#FFFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.84" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PenToolButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Width" Value="48" />
            <Setter Property="Height" Value="48" />
            <Setter Property="Background" Value="#F0FFFFFF" />
            <Setter Property="BorderThickness" Value="1.4" />
        </Style>

        <Style x:Key="SelectableToolButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
        </Style>
    </Application.Resources>
</Application>
